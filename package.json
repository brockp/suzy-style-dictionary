{
	"name": "suzy-style-dictionary",
	"version": "1.0.0",
	"description": "",
	"main": "build.js",
	"scripts": {
		"build": "node ./build.js",
		"build-og": "node_modules/.bin/style-dictionary build",
		"transform": "token-transformer",
		"test": "echo \"Error: no test specified\" && exit 1",
		"watch": "chokidar 'src/**/*.json' -c 'node ./build.js'"
	},
	"author": "Brock Prescott",
	"license": "Apache-2.0",
	"devDependencies": {
		"chokidar-cli": "^1.2.0",
		"handlebars": "^4.0.11",
		"style-dictionary": "^2.10.3",
		"token-transformer": "^0.0.25"
	}
}
